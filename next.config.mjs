/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["randomuser.me"], // Add any other domains you may need here
  },
};

export default nextConfig;
